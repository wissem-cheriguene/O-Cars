{% extends 'baseAdmin.html.twig' %}

{% block title %}Admin des Voitures{% endblock %}

{% block body %}

    <div class="container">

        <br>
        <br>
        <br>



        <h1>Modifier la voiture: </h1>
        <h3>{{ car.title }}</h3>


        {{ form_start(form) }}
        {{ form_row(form.title , {'label': 'Titre'}) }}
        {{ form_row(form.model, {'label':'Modèle' }) }}
        {{ form_row(form.engine, {'label':'Moteur' }) }}
        {{ form_row(form.seat, {'label':'Nombre de sièges' }) }}

        {{ form_row(form.gearbox, {'label': 'boite de vitesse' }) }}
        {{ form_row(form.price, {'label': 'Prix' }) }}
        {{ form_row(form.year, {'label': 'Année' }) }}



        <div class="log">
            <button class="logg btn-lg btn-primary " type="submit">
                Modifier le profil de cet utilisateur
            </button>
        </div>

        {{ form_end(form) }}

    </div>


    <br>
    <br>
    <br>

{% endblock %}
