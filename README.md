# Projet O'Cars

1. Récupération du projet avec git clone
2. `composer install` pour récupérer les dépendances projet (vendors...)
3. lancer l'appli avec le serveur PHP `php -S localhost:8000 -t public` :muscle: